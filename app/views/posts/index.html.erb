<h2>
	Articles
	<% if current_user %>
		<%= link_to image_tag("/images/icons/plus-small.png", :title => "New article", :alt => "+", :border => 0), new_post_path %>
	<% end %>
</h2>

<% for post in @posts %>
	<%= render "post_short", :post => post %>
<% end %>

<div id="pagination">
	<%= will_paginate @posts, :previous_label => "<< Précedent", :next_label => "Suivant >>" %>
</div>