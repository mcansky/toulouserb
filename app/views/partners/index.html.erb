<h2>
	Partenaires
	<% if current_user %>
		<%= link_to image_tag("/images/icons/plus-small.png", :title => "New", :alt => "+", :border => 0), :action => "new" %>
	<% end %>
</h2>

<% for partner in @partners %>
	<%= render "short_partner", :partner => partner %>
<% end %>

<div id="pagination">
	<%= will_paginate @posts, :previous_label => "<< Précedent", :next_label => "Suivant >>" %>
</div>

